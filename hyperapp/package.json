{
  "name": "hyperapp",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.3.4",
    "@babel/plugin-transform-react-jsx": "^7.3.0",
    "babel-preset-env": "^1.7.0",
    "cssnano": "^4.1.10",
    "sass": "^1.17.2"
  },
  "dependencies": {
    "@babel/preset-env": "^7.3.4",
    "airtable": "^0.5.9",
    "hyperapp": "^1.2.9"
  },
  "scripts": {
    "start": "parcel index.html",
    "deploy": "cp now.json ./dist && now ./dist && rm ./dist/now.json",
    "launch": "cp now.json ./dist && now alias && rm ./dist/now.json",
    "now-build": "parcel build index.html"
  }
}
